import { CheckTicketsAvailabiityDirective } from './check-tickets-availabiity.directive';

describe('CheckTicketsAvailabiityDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckTicketsAvailabiityDirective();
    expect(directive).toBeTruthy();
  });
});
